services:
  api:
    build: .
    container_name: olist-dashboard
    restart: always
    ports:
      - "80:8000"
    environment:
      - ENV=production
    volumes:
      - ./dataset:/app/dataset
      - ./models:/app/models
